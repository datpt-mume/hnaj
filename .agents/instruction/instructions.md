# Instruction compatibility note

The active workspace rules live in `.github/copilot-instructions.md` and `.github/instructions/`.

This file is retained only as a pointer for tools that still scan `.agents/instruction/`. Do not add independent rules here; update the `.github/` instruction files instead.