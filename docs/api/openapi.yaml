openapi: 3.1.0
info:
  title: HNaj API
  version: 0.1.0
  description: >-
    Target HTTP contract for HNaj. Operations are documented before domain
    implementation and are not evidence that the current scaffold implements them.
servers:
  - url: http://localhost:8000/api/v1
    description: Local API target
security: []
tags:
  - name: Auth
  - name: Recommendations
  - name: Discovery Options
  - name: Tags
  - name: Places
  - name: Favorites
  - name: Reviews
  - name: Admin Import
paths:
  /auth/csrf-cookie:
    get:
      tags: [Auth]
      operationId: getCsrfCookie
      summary: Bootstrap the Sanctum CSRF cookie
      responses:
        '204': {description: CSRF cookie issued.}
  /auth/password/forgot:
    post:
      tags: [Auth]
      operationId: requestPasswordReset
      summary: Request a password reset link
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PasswordResetRequest'}
      responses:
        '202': {description: Reset request accepted.}
        '422': {$ref: '#/components/responses/ValidationError'}
  /auth/password/reset:
    post:
      tags: [Auth]
      operationId: resetPassword
      summary: Reset a password with a signed token
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PasswordResetConfirmRequest'}
      responses:
        '204': {description: Password reset completed.}
        '422': {$ref: '#/components/responses/ValidationError'}
  /auth/account:
    delete:
      tags: [Auth]
      operationId: deleteAccount
      summary: Delete or anonymize the current account
      security: [{sanctumCookie: []}]
      responses:
        '204': {description: Account deletion completed.}
        '401': {$ref: '#/components/responses/Unauthenticated'}
  /recommendations:
    post:
      tags: [Recommendations]
      operationId: createRecommendation
      summary: Recommend places from a context
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecommendationRequest'
            examples:
              default:
                value:
                  district_id: 01J00000000000000000000000
                  location: {lat: 21.028511, lng: 105.804817}
                  radius_km: 3
                  category_slug: cafe-do-uong
                  price_min: 100000
                  price_max: 300000
                  tags: [hen-ho, trong-nha]
                  limit: 3
      responses:
        '200':
          description: Recommendation result, including an empty result set.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecommendationResponse'
        '422':
          $ref: '#/components/responses/ValidationError'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/InternalError'
  /admin/imports/places/preview:
    post:
      tags: [Admin Import]
      operationId: previewPlaceImport
      summary: Upload and preview a place CSV
      security: [{sanctumCookie: []}]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file: {type: string, format: binary}
      responses:
        '201':
          description: Import preview created.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PlaceImportPreviewResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /admin/imports/places/{batchId}/confirm:
    post:
      tags: [Admin Import]
      operationId: confirmPlaceImport
      summary: Confirm and process a place import
      security: [{sanctumCookie: []}]
      parameters:
        - in: path
          name: batchId
          required: true
          schema: {$ref: '#/components/schemas/Ulid'}
      responses:
        '202':
          description: Import classification and commit accepted.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PlaceImportPreviewResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/InternalError'}
  /admin/imports/places/{batchId}:
    get:
      tags: [Admin Import]
      operationId: getPlaceImport
      summary: Get place import status and staged rows
      security: [{sanctumCookie: []}]
      parameters:
        - in: path
          name: batchId
          required: true
          schema: {$ref: '#/components/schemas/Ulid'}
      responses:
        '200':
          description: Import status.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PlaceImportPreviewResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
  /tags:
    get:
      tags: [Discovery Options]
      operationId: listTags
      summary: List active category taxonomy tags and related discovery suggestions
      parameters:
        - in: query
          name: category
          schema: {type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}
        - in: query
          name: selected[]
          schema:
            type: array
            maxItems: 20
            items: {type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}
      responses:
        '200':
          description: >-
            Ordered active tags. When `category` is provided, tags are limited
            to that category's curated taxonomy; related tag suggestions remain
            based on published-place co-occurrence.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagListResponse'
        '422': {$ref: '#/components/responses/ValidationError'}
  /categories:
    get:
      tags: [Discovery Options]
      operationId: listDiscoveryCategories
      summary: List active discovery categories
      responses:
        '200':
          description: Active categories in display order.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CategoryListResponse'}
  /districts:
    get:
      tags: [Discovery Options]
      operationId: listDiscoveryDistricts
      summary: List active Hanoi districts and available place centroid
      responses:
        '200':
          description: Districts ordered by name.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/DistrictListResponse'}
  /places/{slug}:
    get:
      tags: [Places]
      operationId: getPlaceBySlug
      summary: Get a published place by slug
      parameters:
        - in: path
          name: slug
          required: true
          schema: {type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}
      responses:
        '200':
          description: Place detail.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceResponse'
        '404':
          $ref: '#/components/responses/NotFound'
  /auth/register:
    post:
      tags: [Auth]
      operationId: registerUser
      summary: Register a regular user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: User registered.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/UserResponse'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /auth/login:
    post:
      tags: [Auth]
      operationId: loginUser
      summary: Start an authenticated session
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LoginRequest'}
      responses:
        '200':
          description: Session started.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/UserResponse'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /auth/logout:
    post:
      tags: [Auth]
      operationId: logoutUser
      security: [{sanctumCookie: []}]
      responses:
        '204': {description: Session ended.}
        '401': {$ref: '#/components/responses/Unauthenticated'}
  /auth/me:
    get:
      tags: [Auth]
      operationId: getCurrentUser
      security: [{sanctumCookie: []}]
      responses:
        '200':
          description: Current user.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/UserResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
  /favorites/{placeId}:
    put:
      tags: [Favorites]
      operationId: addFavorite
      security: [{sanctumCookie: []}]
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      responses:
        '204': {description: Favorite created or already existed.}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '404': {$ref: '#/components/responses/NotFound'}
    delete:
      tags: [Favorites]
      operationId: removeFavorite
      security: [{sanctumCookie: []}]
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      responses:
        '204': {description: Favorite removed or already absent.}
        '401': {$ref: '#/components/responses/Unauthenticated'}
  /favorites:
    get:
      tags: [Favorites]
      operationId: listFavorites
      security: [{sanctumCookie: []}]
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PerPage'
      responses:
        '200':
          description: User favorites.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PlaceListResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
  /places/{placeId}/reviews:
    post:
      tags: [Reviews]
      operationId: createReview
      security: [{sanctumCookie: []}]
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ReviewRequest'}
      responses:
        '201':
          description: Review created.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ReviewResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '422': {$ref: '#/components/responses/ValidationError'}
    get:
      tags: [Reviews]
      operationId: listPlaceReviews
      parameters:
        - $ref: '#/components/parameters/PlaceId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PerPage'
      responses:
        '200':
          description: Published reviews.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ReviewListResponse'}
  /reviews/{reviewId}/reports:
    post:
      tags: [Reviews]
      operationId: reportReview
      summary: Report a published review
      security: [{sanctumCookie: []}]
      parameters:
        - $ref: '#/components/parameters/ReviewId'
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ReviewReportRequest'}
      responses:
        '202': {description: Report accepted for moderation.}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '422': {$ref: '#/components/responses/ValidationError'}
  /reviews/{reviewId}/owner-response:
    put:
      tags: [Reviews]
      operationId: upsertOwnerResponse
      summary: Create or replace an owner response
      security: [{sanctumCookie: []}]
      parameters:
        - $ref: '#/components/parameters/ReviewId'
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/OwnerResponseRequest'}
      responses:
        '200':
          description: Owner response saved.
          content:
            application/json:
              schema: {$ref: '#/components/schemas/OwnerResponse'}
        '401': {$ref: '#/components/responses/Unauthenticated'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '422': {$ref: '#/components/responses/ValidationError'}
components:
  securitySchemes:
    sanctumCookie:
      type: apiKey
      in: cookie
      name: XSRF-TOKEN
      description: Laravel Sanctum SPA session. CSRF bootstrap is required.
  parameters:
    Page:
      in: query
      name: page
      schema: {type: integer, minimum: 1, default: 1}
    PerPage:
      in: query
      name: per_page
      schema: {type: integer, minimum: 1, maximum: 100, default: 20}
    PlaceId:
      in: path
      name: placeId
      required: true
      schema: {$ref: '#/components/schemas/Ulid'}
    ReviewId:
      in: path
      name: reviewId
      required: true
      schema: {$ref: '#/components/schemas/Ulid'}
  responses:
    ValidationError:
      description: Validation failed.
      content:
        application/json:
          schema: {$ref: '#/components/schemas/ErrorResponse'}
    Unauthenticated:
      description: Authentication required.
      content:
        application/json:
          schema: {$ref: '#/components/schemas/ErrorResponse'}
    Forbidden:
      description: Authenticated actor lacks permission.
      content:
        application/json:
          schema: {$ref: '#/components/schemas/ErrorResponse'}
    NotFound:
      description: Resource not found.
      content:
        application/json:
          schema: {$ref: '#/components/schemas/ErrorResponse'}
    RateLimited:
      description: Too many requests.
      content:
        application/json:
          schema: {$ref: '#/components/schemas/ErrorResponse'}
    InternalError:
      description: Internal server error.
      content:
        application/json:
          schema: {$ref: '#/components/schemas/ErrorResponse'}
  schemas:
    Ulid:
      type: string
      pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'
    Location:
      type: object
      required: [lat, lng]
      properties:
        lat: {type: number, format: double, minimum: -90, maximum: 90}
        lng: {type: number, format: double, minimum: -180, maximum: 180}
    RecommendationRequest:
      type: object
      required: [location, radius_km]
      properties:
        location: {$ref: '#/components/schemas/Location'}
        district_id: {$ref: '#/components/schemas/Ulid'}
        category_slug: {type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}
        radius_km: {type: number, format: double, minimum: 0.5, maximum: 20}
        price_min: {type: integer, minimum: 0, default: 0, description: Minimum average price per person in VND.}
        price_max: {type: [integer, 'null'], minimum: 0, description: Maximum average price per person in VND; null means no upper bound.}
        tags:
          type: array
          maxItems: 20
          items: {type: string, pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'}
        limit: {type: integer, enum: [1, 3], default: 3}
    RecommendationResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data:
              type: object
              required: [places, meta]
              properties:
                places:
                  type: array
                  items: {$ref: '#/components/schemas/Place'}
                meta: {$ref: '#/components/schemas/RecommendationMeta'}
    RecommendationMeta:
      type: object
      required: [total_matched, fallback_applied, fallback_level, query_radius_km, message_key]
      properties:
        total_matched: {type: integer, minimum: 0}
        fallback_applied: {type: boolean}
        fallback_level: {type: integer, minimum: 0, maximum: 5}
        query_radius_km: {type: number, format: double, minimum: 0.5, maximum: 20}
        message_key: {type: string, example: recommendation.results_found}
        relaxed_tags: {type: boolean, default: false}
    Place:
      type: object
      required: [id, name, slug, price_min, price_max, price_display, distance_km, tags, address, rating]
      properties:
        id: {$ref: '#/components/schemas/Ulid'}
        name: {type: string}
        slug: {type: string}
        cover_image: {type: string, format: uri, nullable: true}
        distance_km: {type: number, format: double}
        price_min: {type: integer}
        price_max: {type: integer}
        price_display: {type: string}
        tags:
          type: array
          items: {$ref: '#/components/schemas/Tag'}
        matched_tags:
          type: array
          items: {type: string}
        address: {type: string}
        rating: {type: number, format: double, minimum: 0, maximum: 5}
    Tag:
      type: object
      required: [id, name, slug, group, sort_order]
      properties:
        id: {$ref: '#/components/schemas/Ulid'}
        name: {type: string}
        slug: {type: string}
        group: {type: string}
        icon: {type: string, nullable: true}
        sort_order: {type: integer}
        is_related: {type: boolean}
    Category:
      type: object
      required: [id, name, slug, sort_order]
      properties:
        id: {$ref: '#/components/schemas/Ulid'}
        name: {type: string}
        slug: {type: string}
        sort_order: {type: integer}
    District:
      type: object
      required: [id, name, slug, center]
      properties:
        id: {$ref: '#/components/schemas/Ulid'}
        name: {type: string}
        slug: {type: string}
        center:
          oneOf:
            - {$ref: '#/components/schemas/Location'}
            - {type: 'null'}
    PlaceImportRow:
      type: object
      required: [id, row_number, status]
      properties:
        id: {$ref: '#/components/schemas/Ulid'}
        row_number: {type: integer, minimum: 1}
        external_id: {type: string, nullable: true}
        status: {type: string, enum: [pending, duplicate, invalid, classified, imported]}
        normalized_data: {type: object, additionalProperties: true}
        classification: {type: object, nullable: true, additionalProperties: true}
        errors:
          type: array
          items: {type: string}
    PlaceImportBatch:
      type: object
      required: [id, filename, status, total_rows, valid_rows, duplicate_rows, invalid_rows, rows]
      properties:
        id: {$ref: '#/components/schemas/Ulid'}
        filename: {type: string}
        status: {type: string, enum: [uploaded, previewed, classifying, completed, cancelled]}
        total_rows: {type: integer, minimum: 0}
        valid_rows: {type: integer, minimum: 0}
        duplicate_rows: {type: integer, minimum: 0}
        invalid_rows: {type: integer, minimum: 0}
        error_message: {type: string, nullable: true}
        rows:
          type: array
          items: {$ref: '#/components/schemas/PlaceImportRow'}
    PlaceImportPreviewResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data: {$ref: '#/components/schemas/PlaceImportBatch'}
    PlaceResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data: {$ref: '#/components/schemas/Place'}
    PlaceListResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data:
              type: object
              properties:
                items: {type: array, items: {$ref: '#/components/schemas/Place'}}
                pagination: {$ref: '#/components/schemas/Pagination'}
    TagListResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data:
              type: object
              required: [tags, related]
              properties:
                tags: {type: array, items: {$ref: '#/components/schemas/Tag'}}
                related: {type: array, maxItems: 6, items: {type: string}}
    CategoryListResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data: {type: array, items: {$ref: '#/components/schemas/Category'} }
    DistrictListResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data: {type: array, items: {$ref: '#/components/schemas/District'} }
    Pagination:
      type: object
      required: [current_page, per_page, total, last_page]
      properties:
        current_page: {type: integer}
        per_page: {type: integer}
        total: {type: integer}
        last_page: {type: integer}
    RegisterRequest:
      type: object
      required: [name, email, password, password_confirmation]
      properties:
        name: {type: string, minLength: 1, maxLength: 255}
        email: {type: string, format: email}
        password: {type: string, minLength: 8, writeOnly: true}
        password_confirmation: {type: string, writeOnly: true}
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: {type: string, format: email}
        password: {type: string, writeOnly: true}
    PasswordResetRequest:
      type: object
      required: [email]
      properties:
        email: {type: string, format: email}
    PasswordResetConfirmRequest:
      type: object
      required: [token, email, password, password_confirmation]
      properties:
        token: {type: string, writeOnly: true}
        email: {type: string, format: email}
        password: {type: string, minLength: 8, writeOnly: true}
        password_confirmation: {type: string, writeOnly: true}
    User:
      type: object
      required: [id, name, email, roles]
      properties:
        id: {$ref: '#/components/schemas/Ulid'}
        name: {type: string}
        email: {type: string, format: email}
        roles: {type: array, items: {type: string}}
    UserResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data: {$ref: '#/components/schemas/User'}
    ReviewRequest:
      type: object
      required: [rating]
      properties:
        rating: {type: integer, minimum: 1, maximum: 5}
        body: {type: string, maxLength: 5000}
        experience_tags: {type: array, items: {type: string}}
        media_ids: {type: array, items: {$ref: '#/components/schemas/Ulid'}}
    Review:
      allOf:
        - {$ref: '#/components/schemas/ReviewRequest'}
        - type: object
          required: [id, user_id, created_at]
          properties:
            id: {$ref: '#/components/schemas/Ulid'}
            user_id: {$ref: '#/components/schemas/Ulid'}
            created_at: {type: string, format: date-time}
    ReviewResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data: {$ref: '#/components/schemas/Review'}
    ReviewReportRequest:
      type: object
      required: [reason]
      properties:
        reason: {type: string, maxLength: 100}
        details: {type: string, maxLength: 2000}
    OwnerResponseRequest:
      type: object
      required: [body]
      properties:
        body: {type: string, minLength: 1, maxLength: 5000}
    OwnerResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data:
              type: object
              required: [id, review_id, user_id, body, created_at]
              properties:
                id: {$ref: '#/components/schemas/Ulid'}
                review_id: {$ref: '#/components/schemas/Ulid'}
                user_id: {$ref: '#/components/schemas/Ulid'}
                body: {type: string}
                created_at: {type: string, format: date-time}
    ReviewListResponse:
      allOf:
        - {$ref: '#/components/schemas/SuccessResponse'}
        - type: object
          properties:
            data:
              type: object
              properties:
                items: {type: array, items: {$ref: '#/components/schemas/Review'}}
                pagination: {$ref: '#/components/schemas/Pagination'}
    SuccessResponse:
      type: object
      required: [success, data]
      properties:
        success: {const: true}
        data: {}
    ErrorDetail:
      type: object
      required: [field, message_key]
      properties:
        field: {type: string}
        message_key: {type: string}
    ErrorResponse:
      type: object
      required: [success, error]
      properties:
        success: {const: false}
        error:
          type: object
          required: [code, message_key]
          properties:
            code: {type: string}
            message_key: {type: string}
            details: {type: array, items: {$ref: '#/components/schemas/ErrorDetail'}}
